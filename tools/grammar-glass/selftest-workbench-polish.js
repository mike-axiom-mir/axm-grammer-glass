'use strict';
const assert=require('assert'),Core=require('./workbench-polish-core.js'),sha=require('./playground-core.js').sha256;
const cmp=sha('cmp'),cycle=sha('cycle'),rippleSha=sha('ripple'),importA=sha('a'),importB=sha('b');
const base={glassLoaded:true,documentVisible:true,motionState:{motionEnabled:true},evolutionState:{primaryLoaded:true,ghostLoaded:true,selectedLensIds:['SEMANTIC_DIRECTIONS'],blend:.5,changeCount:3},visualPlan:{comparisonSha256:cmp,changedLayerCount:2,totalRenderedMarks:44,selectedLensIds:['SEMANTIC_DIRECTIONS'],primaryBinding:{importReceiptSha256:importA,sourceCommitSha:'1'.repeat(40)},ghostBinding:{importReceiptSha256:importB,sourceCommitSha:'2'.repeat(40)}},overlayBinding:{glassBinding:{cycleSha256:cycle}},rippleReceipt:{result:'GRAMMAR_102_CHANGE_RIPPLE_READY_STRUCTURAL_CONTACT_ONLY',changeRippleSha256:rippleSha,origin:{lensId:'SEMANTIC_DIRECTIONS',itemId:'html:K01'},sourceBinding:{generationComparisonSha256:cmp,glass:{cycleSha256:cycle}},truth:{structuralContactNotCausalProof:true}},rippleState:{activeLensId:'SEMANTIC_DIRECTIONS',activeItemId:'html:K01',depth:3,changeCount:3,comparisonSha256:cmp}};
const ready=Core.createHandoff(base);assert.strictEqual(ready.result,'WORKBENCH_READY');assert.strictEqual(ready.health.staleReasons.length,0);assert.strictEqual(ready.health.checks.rippleReady,true);assert.strictEqual(ready.truth.readinessIsNotCorrectness,true);assert.deepStrictEqual(Core.createHandoff(base),ready);
const staleCmp=Core.createHandoff({...base,visualPlan:{...base.visualPlan,comparisonSha256:sha('new')}});assert.strictEqual(staleCmp.result,'HELD_STALE_VIEW_STATE');assert(staleCmp.health.staleReasons.includes('RIPPLE_COMPARISON_STALE'));
const staleLens=Core.createHandoff({...base,evolutionState:{...base.evolutionState,selectedLensIds:['BASE_GRAMMAR']},visualPlan:{...base.visualPlan,selectedLensIds:['BASE_GRAMMAR']}});assert.strictEqual(staleLens.result,'HELD_STALE_VIEW_STATE');assert(staleLens.health.staleReasons.includes('RIPPLE_LENS_NO_LONGER_SELECTED'));
const partial=Core.createHandoff({glassLoaded:true,evolutionState:{primaryLoaded:true,ghostLoaded:false,selectedLensIds:[]},motionState:{motionEnabled:false},documentVisible:false});assert.strictEqual(partial.result,'WORKBENCH_PARTIAL');assert.strictEqual(partial.health.stages.find(x=>x.stage==='LOAD_102_B').state,'WAIT');assert.strictEqual(partial.runtime.documentVisible,false);
console.log(JSON.stringify({ok:true,schema:ready.schema,result:ready.result,handoffSha256:ready.handoffSha256,staleChecks:2,authority:ready.truth.authority},null,2));
